[tool.poetry]
name = "loading-programs"
version = "1.0.0"
description = "Matrix data analysis tool (pip vs Poetry demonstration)"
authors = ["Zion Data Engineer <redpill@zion.io>"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
pandas = ">=2.0"
numpy = ">=1.24"
matplotlib = ">=3.7"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
